<UserControl x:Class="Memory_Game.Controls.CardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Button
        x:Name="CardButton"
        Margin="10"
        BorderThickness="0"
        Background="Transparent"
        Cursor="Hand">

        <Button.Template>

            <ControlTemplate TargetType="Button">

                <ContentPresenter/>

            </ControlTemplate>

        </Button.Template>

        <Image
    x:Name="CardImage"
    Source="/Assets/Cards/card-back.png"
    Stretch="Uniform"
    RenderTransformOrigin="0.5,0.5">

            <Image.RenderTransform>

                <ScaleTransform x:Name="CardScale"/>

            </Image.RenderTransform>

        </Image>

    </Button>

</UserControl>